


There are two different structs that stores position,
which both are necessary.

Position is in millimeters, and uses floats
point is in centimeters, and uses signed ints.

point will eventually be refactored to use Unsigned



